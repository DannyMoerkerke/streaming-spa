# Streaming SPA
A streaming SPA built using [Declarative partial updates](https://developer.chrome.com/blog/declarative-partial-updates).

Run `npm start` to start the server on localhost:8888.

Works in Chromium-based browsers version 148 with experimental web features enabled.
